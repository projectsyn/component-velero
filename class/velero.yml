parameters:
  kapitan:
    compile:
      - input_paths:
          - velero/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - velero/component/main.jsonnet
        input_type: jsonnet
        output_path: velero/
